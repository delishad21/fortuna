# Fortuna brand

`fortuna.png` is the original supplied attachment, copied without image modification.
Run `npm run brand:sync` inside `mobile/` to synchronize both apps.
The web UI uses the shared `BrandMark` component; Expo uses the image for launcher/adaptive icons and splash. Next.js serves PNG favicon and Apple icon routes.
